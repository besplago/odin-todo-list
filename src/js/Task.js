export class Task {
  constructor(title, completed, important, dueDate, notes) {
    this.title = title;
    this.completed = completed;
    this.important = important;
    this.dueDate = dueDate;
    this.notes = notes;
  }
}
